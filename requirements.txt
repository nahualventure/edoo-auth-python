# Core runtime — JWKS validation only (Finance, Admissions)
PyJWT>=2.9.0
cryptography>=43.0.0

# Optional — required for edoo-auth[oidc] (full OIDC flow, Edoo SIS)
# httpx>=0.27.0

# Optional — required for edoo-auth[django]
# Django>=4.2
# djangorestframework>=3.14
